name: conda
channels:
  - defaults
  - conda-forge
dependencies:
  - conda
  - mamba
  - micromamba
  - conda-build
  - conda-verify
  - notebook
  - jupyter_client
  - ipykernel
  - pytest
  - pytest-cov 
  - requests 
  - mock
